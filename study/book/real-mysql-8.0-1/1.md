---
tags:
  - db
  - study
---

# 내부 구조 정리 1탄

#### 자동 데드락의 원인

교착에 빠진 트랜잭션을 종료하는데 그 기준은 언두 로그

더 적은 애들이 롤백 대상

데드락 감지 스레드 매우 많아지면 존나 아픔

데드락 감지 스레드를 비활성화 하는것도 좋은 방법이 될수있다.

이럴경우 무한정 대기하고 하지만 타임아웃 설정하고, 에러반환하는것도 가능하다.<br>

#### 버퍼풀

디스크의 데이터 파일이나 인덱스 정보를 메모리에 캐시해 두는 공간

물리 메모리에 레코드 버퍼가 사용되는 구조일수 있다 .

따라서 버퍼풀의 크기를 동적으로 조절할수있게 하는것도 방법이다 .

버퍼풀은 많으면 좋긴 하지만 그에 따른 클라이언트 스레드 한테 도움을 줘야한다.

버퍼풀 쪼개어 관리 한다.

버퍼풀은 LRU and list , flush list and free list

버퍼풀은 캐시와 쓰기 버퍼링 두가지 용도가 있다 .

단순히 늘리면 캐시 기능만 향상 한다.

free 리스트는 실제 사용자 데이터가 없는 빈 페이지 목록 , 사용자 쿼리가 새롭게 디스크의 데이터 페이지를 읽어와야 하는 경우 사용\
올드 서비스 리스트 영역은 LRU , 뉴 서브리스트 MRU, LRU 리스트의 목적은 디스크에 읽어온 페이지를 오랫동안 유지해서 디스크 읽기를 최소화 하는 것이 목표이다 .

innoDB 에서 데이터를 찾는 과정 (자세한 설명 필요)

1. 필요한 레코드 데이터 페이지가 버퍼풀에 있는지 확인&#x20;
2. 디스크에 필요한 페이지  버퍼풀에 적재 , 포인트를 LRU 헤더에 추가&#x20;
3. 적재된 페이지 읽히면 MRU 로 이동&#x20;
4. 오래 머무는 기간에 따라 나이를 먹고 그만큼 MRU 에 유배&#x20;



플러시 리스트

디스크로 동기화 하지 않는 데이터를 가진 페이지의 변경 시점 기준의 페이지 목록을 관리

일단 한번이라도 변경된 데이터 페이지는 플러시 리스트에 관리되고 특정 시점에 기록 (자세한 내용 좀 있음)





#### 버퍼풀과 redo 로그

쓰기를 늘릴려면 버퍼링 기능까지 향상 시켜야한다. 명령어 실행 으로 변경 된 데이터를 가진 더티 페이지 가 있다 .

리두 로그는 한개 이상의 고정크기 파일을 연결해서 순환 고리 처럼 사용한다. 데이터가 계속해서 변경하면 리두 로그는 계속 덮어씌여야한다,&#x20;

스토리지 엔진은 주기적으로 체크포인트 이벤트를 발생시켜 리두로그와 데티페이지를 디스크에 동기화한다.&#x20;

더티페이지 비율과 리두 로그파일의 전체 크기를 이해해야한다.&#x20;

&#x20;버퍼풀과 리두로그 엔트리와 관계를 판단하여 진행한다. 100기가 기준 리두 로그 파일의 전체 크기를 대략 5\~10GB 수준으로 선택하고 필요할때 마다 늘려라&#x20;



버퍼 풀 플러시 : 버퍼풀에서 변경된 데이터를 실제 데이터 파일 디스크에 안전하고 효율적으로 반영하는 과정

결국 플러시는 메모리 청소하는것이 아니라 , redo log 공간 확보, 버퍼풀 여우 페이지 확보 , 쓰기 부하 평탄화 하는 과정이이다 .&#x20;



#### 더블 라이트 버퍼

더티페이지를 실제 반영하기 전에 안전하게 임시 공간에 한번 기록을하는것 , 결국 문제가 생겼을때 , 저장에 실패했을때만 사용하는 방식이다.&#x20;



### 언두로그

트랜잭션과 격리수준을 보장하기 위해 이전 버전의 데이터를 별도로 백업ㅎ을 하ㅏㄴ다 .

* 트랜잭션 보장,&#x20;
* 격리수준 보장 - 특정 커넥션에서 데이터 변경할때 다른 커넥션에서 조회 하면 격리수준에 맞게 언두 로그에 백업해둔 데이터 읽어서 반환



결국 안정적인 롤백과 안정적인 동시성을 위한 개념이라고 보면 된다.&#x20;



이전 5.5 에서는 undo 공간이 다시 줄어들지 않았다.&#x20;

트랜잭션이 오랫동안 실행 된다면 그만큼의 로그양도 늘어나서 큰일남&#x20;

로그양이 늘어나는 문제보다는 그만큼 변경된 이력을 스캔해야하는 부담으로 인해 쿼리 성능의 전반적인 문제가 될수있다.&#x20;

undo 테이블 스페이스 한번 조사 해보기 (128p)



### 체인지 버퍼

데이터 파일 변경 할때 해당 테이블의 인덱스를 업데이트 하는 작업도 필요하다. 만약 인덱스가 많으면 개빡셀수있다. 그래서 변경해야할 인덱스 페이지가 버퍼풀에 있으면 바로 업데이트를 수행하지만 그렇지 않고 디스크로 땡겨져 왔다면 즉시 실행하지않고 임시 공간에 저장해 바로 사용자에게 반환 결과를 반환하는 형태로 성능 항상 시키는데 , 이때 이걸 체인지 버퍼이다.&#x20;



단 유니크 인덱스는 체인지 버퍼를 사용할수없다. 왜냐면 중복 여부를 체크해줘야 하기 때문이다. 체인지 버퍼도 최대 25% 부터 50% 까지 할수있다.&#x20;



리두랑 언두는 결국 목적이 다르다 . 아직 반영 안되어야 하는거는 undo , 문제 생길때 실시간으로 기록하고 다사 해야하는건 redo&#x20;



### 어댑티브 해시 인덱스

코어에서 자주 요청하는 데이터에 대해 자동 생성하는 인덱스&#x20;

b- 가 빠르다 느리다라고 할수있는다 디비의 어떤 특정 작업이 많으냐에 따라 느릴수도 있고 빠를수있다.&#x20;



그렇다면 AHI 랑 B + 랑은 근본이 다른걸까?&#x20;

근본이 다르다 . 이것도 결국 20% 이상 등가를 내려가면 유지 비용이기 때문에 끄는걸 검토할수있다.&#x20;

이런거는 보통&#x20;

디스크 읽기가 많은 경우&#x20;

특정 패턴의 쿼리가 많은 경우 , 매우 큰 데이터를 가진 테이블의 레코드를 읽는경우&#x20;



반대로 좋은건&#x20;

디스크 데이터가 버퍼풀 크기와 비슷한 경우 (읽기 많은 경우)\
동등 조건 검색 이 많은경우 \
쿼리에서 일부 데이터만 집중되는 경우

결국 이것도 적당 한 트레이드 오프를 한느것이 맞지 않나 싶다.&#x20;
